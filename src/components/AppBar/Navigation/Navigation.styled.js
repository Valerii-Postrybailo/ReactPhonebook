import { Button } from '@mui/material';
// import { NavLink } from 'react-router-dom';
import styled from 'styled-components';

export const NavLinkStyled = styled(Button)(theme => {
  // (
  //   {
  //     '&.active': {
  //       backgroundColor: theme.palette.success.main,
  //     }
  //   })
});